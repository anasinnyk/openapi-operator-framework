// @generated by `cargo run -p xtask -- generate-cloudflare-sdk`
#![allow(dead_code)]

use std::{sync::Arc, time::Duration};

use k8s_openapi::api::core::v1::Secret;
use kube::{
    Api, Resource, ResourceExt,
    api::{Patch, PatchParams},
    runtime::controller::Action,
};
use reqwest::header::{AUTHORIZATION, HeaderMap, HeaderValue};
use serde_json::{Map, Value, json};

use crate::{
    cloudflare::CloudflareError,
    controllers::{ControllerContext, ControllerError},
    resources::{Account, Zone, ZoneRuleset, ZoneRulesetStatus},
};

const FINALIZER: &str = "cloudflare.cfrs.dev/zone-ruleset";
const EXTERNAL_ID_ANNOTATION: &str = "cloudflare.cfrs.dev/external-id";
const CLOUDFLARE_API_BASE_URL: &str = "https://api.cloudflare.com/client/v4";

// OpenAPI controller plan for ZoneRuleset:
// - create: POST /zones/{zone_id}/rulesets (zone-rulesets-create)
//   kind: ZoneRuleset
//   scope: namespaced
//   operation: create
//   owner:
//     kind: Zone
//     ref: spec.zoneRef
//   identity:
//     statusField: status.id
//     response: result.id
//   params:
//     zone_id: owner.status.id
//   body:
//     name: spec.name
//     kind: spec.kind
//     phase: spec.phase
//     description: spec.description
//     rules: spec.rules
// - delete: DELETE /zones/{zone_id}/rulesets/{ruleset_id} (zone-rulesets-delete)
//   kind: ZoneRuleset
//   scope: namespaced
//   operation: delete
//   owner:
//     kind: Zone
//     ref: spec.zoneRef
//   params:
//     zone_id: owner.status.id
//     ruleset_id: status.id
// - read: GET /zones/{zone_id}/rulesets/{ruleset_id} (zone-rulesets-read)
//   kind: ZoneRuleset
//   scope: namespaced
//   operation: read
//   owner:
//     kind: Zone
//     ref: spec.zoneRef
//   params:
//     zone_id: owner.status.id
//     ruleset_id: status.id
// - update: PUT /zones/{zone_id}/rulesets/{ruleset_id} (zone-rulesets-update)
//   kind: ZoneRuleset
//   scope: namespaced
//   operation: update
//   owner:
//     kind: Zone
//     ref: spec.zoneRef
//   params:
//     zone_id: owner.status.id
//     ruleset_id: status.id
//   body:
//     name: spec.name
//     kind: spec.kind
//     phase: spec.phase
//     description: spec.description
//     rules: spec.rules

pub(in crate::controllers) async fn reconcile(
    zone_ruleset: Arc<ZoneRuleset>,
    context: Arc<ControllerContext>,
) -> Result<Action, ControllerError> {
    let namespace = zone_ruleset
        .namespace()
        .ok_or_else(|| ControllerError::MissingNamespace {
            kind: "ZoneRuleset",
            name: zone_ruleset.name_any(),
        })?;

    if zone_ruleset.meta().deletion_timestamp.is_some() {
        delete_external(&zone_ruleset, &context, &namespace).await?;
        remove_finalizer(&zone_ruleset, &context, &namespace).await?;
        return Ok(Action::await_change());
    }

    ensure_finalizer(&zone_ruleset, &context, &namespace).await?;

    let zone = load_zone(&zone_ruleset, &context, &namespace).await?;
    let zone_id = zone
        .status
        .as_ref()
        .map(|status| status.id.as_str())
        .ok_or_else(|| ControllerError::MissingStatusId {
            kind: "Zone",
            name: zone.name_any(),
        })?;
    let account = load_account(&zone, &context, &namespace).await?;
    let api_token = load_api_token(&account, &context, &namespace).await?;
    let client = sdk_client(&api_token)?;

    let external_id = zone_ruleset
        .status
        .as_ref()
        .map(|status| status.id.as_str())
        .or_else(|| external_id(&zone_ruleset));

    let body = body(zone_ruleset.as_ref());
    let response = match external_id {
        Some(external_id) => {
            let record_id = external_id.try_into().map_err(|error| {
                ControllerError::Cloudflare(CloudflareError::Api(format!("{error}")))
            })?;
            let _current = client
                .zone_rulesets_read(
                    &zone_id.try_into().map_err(|error| {
                        ControllerError::Cloudflare(CloudflareError::Api(format!("{error}")))
                    })?,
                    &record_id,
                )
                .await
                .map_err(|error| {
                    ControllerError::Cloudflare(CloudflareError::Api(format!("{error:?}")))
                })?;
            client
                .zone_rulesets_update(
                    &zone_id.try_into().map_err(|error| {
                        ControllerError::Cloudflare(CloudflareError::Api(format!("{error}")))
                    })?,
                    &record_id,
                    None,
                    &body,
                )
                .await
                .map_err(|error| {
                    ControllerError::Cloudflare(CloudflareError::Api(format!("{error:?}")))
                })?
                .into_inner()
        }
        None => client
            .zone_rulesets_create(
                &zone_id.try_into().map_err(|error| {
                    ControllerError::Cloudflare(CloudflareError::Api(format!("{error}")))
                })?,
                None,
                &body,
            )
            .await
            .map_err(|error| {
                ControllerError::Cloudflare(CloudflareError::Api(format!("{error:?}")))
            })?
            .into_inner(),
    };

    patch_status(&zone_ruleset, &context, &namespace, &response).await?;

    Ok(Action::requeue(Duration::from_mins(5)))
}

fn body(zone_ruleset: &ZoneRuleset) -> Map<String, Value> {
    let value = json!({
        "name": zone_ruleset.spec.name,
        "kind": zone_ruleset.spec.kind,
        "phase": zone_ruleset.spec.phase,
        "description": zone_ruleset.spec.description,
        "rules": zone_ruleset.spec.rules,
    });
    match value {
        Value::Object(map) => map,
        _ => Map::new(),
    }
}

async fn delete_external(
    zone_ruleset: &ZoneRuleset,
    context: &ControllerContext,
    namespace: &str,
) -> Result<(), ControllerError> {
    let Some(status) = &zone_ruleset.status else {
        return Ok(());
    };
    let zone = load_zone(zone_ruleset, context, namespace).await?;
    let zone_id = zone
        .status
        .as_ref()
        .map(|status| status.id.as_str())
        .ok_or_else(|| ControllerError::MissingStatusId {
            kind: "Zone",
            name: zone.name_any(),
        })?;
    let account = load_account(&zone, context, namespace).await?;
    let api_token = load_api_token(&account, context, namespace).await?;
    let client = sdk_client(&api_token)?;
    client
        .zone_rulesets_delete(
            &zone_id.try_into().map_err(|error| {
                ControllerError::Cloudflare(CloudflareError::Api(format!("{error}")))
            })?,
            &status.id.as_str().try_into().map_err(|error| {
                ControllerError::Cloudflare(CloudflareError::Api(format!("{error}")))
            })?,
            None,
        )
        .await
        .map_err(|error| ControllerError::Cloudflare(CloudflareError::Api(format!("{error:?}"))))?;
    Ok(())
}

async fn load_zone(
    zone_ruleset: &ZoneRuleset,
    context: &ControllerContext,
    namespace: &str,
) -> Result<Zone, ControllerError> {
    let zones = Api::<Zone>::namespaced(context.client.clone(), namespace);
    Ok(zones.get(&zone_ruleset.spec.zone_ref.name).await?)
}

async fn load_account(
    zone: &Zone,
    context: &ControllerContext,
    namespace: &str,
) -> Result<Account, ControllerError> {
    let accounts = Api::<Account>::namespaced(context.client.clone(), namespace);
    Ok(accounts.get(&zone.spec.account_ref.name).await?)
}

async fn load_api_token(
    account: &Account,
    context: &ControllerContext,
    namespace: &str,
) -> Result<String, ControllerError> {
    let secret_ref = &account.spec.api_token.secret_ref;
    let secrets = Api::<Secret>::namespaced(context.client.clone(), namespace);
    let secret = secrets.get(&secret_ref.name).await?;
    let data = secret
        .data
        .as_ref()
        .and_then(|data| data.get(&secret_ref.key))
        .ok_or_else(|| ControllerError::MissingSecretKey {
            name: secret_ref.name.clone(),
            key: secret_ref.key.clone(),
        })?;
    String::from_utf8(data.0.clone())
        .map(|token| token.trim().to_owned())
        .map_err(|error| ControllerError::SecretValueUtf8 {
            name: secret_ref.name.clone(),
            key: secret_ref.key.clone(),
            error,
        })
}

fn sdk_client(
    api_token: &str,
) -> Result<crate::cloudflare::generated::zone_ruleset::Client, ControllerError> {
    let mut headers = HeaderMap::new();
    let mut authorization = HeaderValue::from_str(&format!("Bearer {api_token}"))?;
    authorization.set_sensitive(true);
    headers.insert(AUTHORIZATION, authorization);
    let http = reqwest::Client::builder()
        .default_headers(headers)
        .build()?;
    Ok(
        crate::cloudflare::generated::zone_ruleset::Client::new_with_client(
            CLOUDFLARE_API_BASE_URL,
            http,
        ),
    )
}

async fn patch_status(
    zone_ruleset: &ZoneRuleset,
    context: &ControllerContext,
    namespace: &str,
    response: &Map<String, Value>,
) -> Result<(), ControllerError> {
    let id = response
        .get("result")
        .and_then(|result| result.get("id"))
        .and_then(Value::as_str)
        .ok_or(CloudflareError::MissingResult("zone_rulesets_create"))?;
    let resources = Api::<ZoneRuleset>::namespaced(context.client.clone(), namespace);
    let status = ZoneRulesetStatus {
        id: id.to_owned(),
        observed_generation: zone_ruleset.metadata.generation,
        ..Default::default()
    };
    resources
        .patch_status(
            &zone_ruleset.name_any(),
            &PatchParams::apply("cloudflare-provider"),
            &Patch::Apply(json!({
                "apiVersion": "cloudflare.cfrs.dev/v1alpha1",
                "kind": "ZoneRuleset",
                "status": status,
            })),
        )
        .await?;
    Ok(())
}

async fn ensure_finalizer(
    zone_ruleset: &ZoneRuleset,
    context: &ControllerContext,
    namespace: &str,
) -> Result<(), ControllerError> {
    let mut finalizers = zone_ruleset.meta().finalizers.clone().unwrap_or_default();
    if finalizers.iter().any(|finalizer| finalizer == FINALIZER) {
        return Ok(());
    }
    finalizers.push(FINALIZER.to_owned());
    patch_finalizers(zone_ruleset, context, namespace, finalizers).await
}

async fn remove_finalizer(
    zone_ruleset: &ZoneRuleset,
    context: &ControllerContext,
    namespace: &str,
) -> Result<(), ControllerError> {
    let mut finalizers = zone_ruleset.meta().finalizers.clone().unwrap_or_default();
    finalizers.retain(|finalizer| finalizer != FINALIZER);
    patch_finalizers(zone_ruleset, context, namespace, finalizers).await
}

async fn patch_finalizers(
    zone_ruleset: &ZoneRuleset,
    context: &ControllerContext,
    namespace: &str,
    finalizers: Vec<String>,
) -> Result<(), ControllerError> {
    let resources = Api::<ZoneRuleset>::namespaced(context.client.clone(), namespace);
    resources
        .patch(
            &zone_ruleset.name_any(),
            &PatchParams::default(),
            &Patch::Merge(json!({ "metadata": { "finalizers": finalizers } })),
        )
        .await?;
    Ok(())
}

fn external_id(zone_ruleset: &ZoneRuleset) -> Option<&str> {
    zone_ruleset
        .meta()
        .annotations
        .as_ref()?
        .get(EXTERNAL_ID_ANNOTATION)
        .map(String::as_str)
        .filter(|external_id| !external_id.is_empty())
}

#[allow(clippy::needless_pass_by_value)]
pub(in crate::controllers) fn error_policy(
    zone_ruleset: Arc<ZoneRuleset>,
    error: &ControllerError,
    _context: Arc<ControllerContext>,
) -> Action {
    tracing::warn!(resource = %zone_ruleset.name_any(), kind = "ZoneRuleset", ?error, "scheduling generated controller retry after reconciliation error");
    Action::requeue(Duration::from_mins(1))
}
